# REQ-007: Coverage >= 80% on all packages

**Status**: Implemented
**Phase**: 0
**Priority**: Must
**Source**: Development setup plan

## Description

Test coverage must be maintained at 80% or above across all packages. The coverage
check must be enforced in CI and as part of the release gate.

## Acceptance Criteria

- [x] `hack/coverage.sh` enforces minimum coverage
- [x] `make cover-check` fails if coverage < 80%
- [x] `make ci` includes cover-check
- [x] `make release-check` includes cover-check
- [x] CI workflow runs cover-check

## Linked

- hack/coverage.sh
- .github/workflows/ci.yml
